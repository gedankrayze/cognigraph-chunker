# Pin the toolchain so a new stable release cannot turn fresh clippy lints
# into CI failures. Bump deliberately (and fix any new lints in the same PR).
[toolchain]
channel = "1.98.1"
components = ["rustfmt", "clippy"]
